{% set data = load_setup_py_data() %}

package:
    name: climtas
    version: 0.2.1 # {{ data.get('version') }}

source:
    path: .


build:
    script: "{{ PYTHON }} -m pip install . --no-deps"
    noarch: python
    number: 1 # {{ GIT_DESCRIBE_NUMBER }}


requirements:
    host:
        - python
        - pip
    run:
        - python
        - dask
        - netcdf4
        - pandas
        - scipy
        - tqdm
        - xarray
        - typing_extensions
        - iris
        - cfunits
        - mule
        - sparse

test:
    imports:
        - climtas
    requires:
        - pytest
        - coverage
        - cdo
        - esmf
        - hdf5=1.10.5 # Missing CDO dependency
    files:
        - setup.cfg
        - test
        - README.rst
        - doc
    script_env:
        - TEST_OUTPUT
    commands:
        - COVERAGE_FILE=${TEST_OUTPUT:-.}/coverage coverage run --source climtas -m pytest ./test --pyargs climtas --junit-xml=${TEST_OUTPUT:-.}/pytest/junit.xml

